PORT=3000
NODE_ENV=development

# Optional. Without this key, the API returns deterministic draft suggestions
# so local development and tests do not call a model provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Comma-separated browser origins allowed to call the API.
CORS_ORIGIN=http://localhost:3000
